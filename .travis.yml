dist: bionic
services:
  - xvfb
language: node_js
node_js:
  - "node"
addons:
  apt:
    sources:
      - google-chrome
    packages:
    - google-chrome-stable

before_script:
  - export CHROME_BIN=chromium-browser